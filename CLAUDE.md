# 프로젝트: 청약 알림봇 (apply-alert-bot)

## 목표

한국부동산원 청약홈 공식 오픈API(공공데이터포털)를 폴링해서, 관심
지역(수원 영통구, 용인 기흥구)에 신규 아파트 분양 공고가 뜨면 이메일
로 알려주는 봇을 완성한다. 텔레그램 봇이 없어서 SMTP 이메일 발송으로
결정했다. 크롤링이 아니라 승인받은 공식 API 기반
이며, 이후 2단계로 부동산 커뮤니티/건설사 사전 홍보 크롤러를 별도
레이어로 추가할 계획이다(지금 단계에서는 다루지 않는다).

## 현재 상태

Go로 뼈대 코드가 이미 작성되어 있고 `go build ./...`까지 통과한
상태다. 외부 모듈 의존성은 없음(stdlib만 사용, DB 대신 JSON 파일로
중복 방지). 구조:

- `main.go` — 폴링 루프, 지역 필터링, 알림 발송
- `internal/api/client.go`, `internal/api/types.go` — 청약홈 "APT
  분양정보 상세조회" API(`getAPTLttotPblancDetail`) 클라이언트
- `internal/store/store.go` — JSON 파일 기반 중복 알림 방지
- `internal/notify/email.go` — SMTP(`net/smtp`)로 이메일 발송 (네이버
  메일 기준으로 검증 예정, STARTTLS 자동 협상)
- `cmd/inspect/main.go` — 실제 API 원본 JSON을 그대로 덤프해서
  `types.go` 태그와 대조하는 검증용 커맨드
- `.github/workflows/poll.yml` — GitHub Actions cron으로 상시 구동
  (아래 3번 항목 참고)
- `.env.example`, `README.md`

API 서비스키 2종은 공공데이터포털에서 이미 활용신청 승인받은 상태:
- 한국부동산원_청약홈 분양정보 조회 서비스
- 한국부동산원_청약홈 청약접수 경쟁률 및 특별공급 신청현황 조회 서비스

## 지금 해야 할 작업 (우선순위 순)

1. **실제 API 응답으로 필드 검증** — 승인된 서비스키로
   `getAPTLttotPblancDetail`을 직접 호출해서 실제 응답 JSON을 받아보고,
   `internal/api/types.go`의 struct 태그(`PBLANC_NO`, `HOUSE_NM`,
   `SUBSCRPT_AREA_CODE_NM` 등)가 정확히 일치하는지 대조·수정한다.
   특히 **"공급위치(상세주소)"에 해당하는 필드가 실제로 존재하는지
   확인**하는 게 중요하다 — 현재 `SUBSCRPT_AREA_CODE_NM`은 "경기" 같은
   시/도 단위라서 "수원", "용인" 같은 시/구 단위 필터링이 안 될 수
   있다. 있다면 struct에 필드를 추가하고 `matchesRegion` 로직도 그
   필드를 우선 사용하도록 바꿔라.

2. **이메일 알림 실제 동작 확인** — 네이버 메일 SMTP 계정 정보(앱
   비밀번호 포함)를 받아 `.env`를 채운 뒤 `go run .`으로 한 번 돌려서
   실제 메일이 오는지 확인하고, 에러 케이스(잘못된 비밀번호, 네트워크
   실패 등)도 한 번씩 유발해서 로그와 실패 알림이 의도대로 동작하는지
   본다.

3. **상시 구동 방식 결정 및 구성** — GitHub Actions scheduled
   workflow(`.github/workflows/poll.yml`)로 결정·구성 완료. 로컬 상시
   실행이나 VM/라즈베리파이 systemd는 검토했으나, 별도 하드웨어 유지
   보수가 없고 이미 쓰는 리포지토리 안에서 완결된다는 이유로 채택하지
   않았다. (내 사내 환경은 NC소프트이므로 회사 인프라는 쓰지 않고
   개인 환경에 올릴 것 — 이 제약을 충족한다.) `seen.json`은 매 실행 후
   워크플로우가 직접 리포지토리에 커밋해서 상태를 유지한다.

4. **경쟁률 API 연동 (선택, 우선순위 낮음)** — "청약접수 경쟁률 및
   특별공급 신청현황 조회 서비스"는 접수 마감 후 결과 확인용이라 MVP
   알림에는 필수가 아니다. 여유가 되면 별도 명령어나 주기 작업으로
   붙인다. 엔드포인트명은 `ApplyhomeInfoDetailSvc`/`ApplyhomeInfoCmpttRtSvc`
   계열 네이밍 규칙을 참고해 공공데이터포털 Swagger 문서에서 직접
   확인해라(추측하지 말 것).

## 코드 스타일

- 표준 Go 관례를 따른다: `gofmt`/`go vet` 통과, 에러는 감추지 말고
  로그 남기거나 호출자에게 전파, 패키지는 책임 단위로 분리된 현재
  구조 유지.
- 외부 의존성 추가는 신중하게 — 지금은 stdlib만으로 충분히 돌아가는
  상태이므로, 꼭 필요한 경우가 아니면 늘리지 않는다.
- 변경할 때마다 `go build ./...`와 `go vet ./...`로 확인한다.

## 참고

- API 데이터가 청약홈 사이트 자체보다 빠르지 않다는 걸 이미 확인했다
  (같은 시스템의 데이터를 노출하는 구조). 이 봇의 가치는 "속도"가
  아니라 "매일 직접 확인하지 않아도 된다"는 자동화에 있다.
- 목표 지역: 수원 영통구, 용인 기흥구 (주택 매수 목표 시점 2027년
  5월 참고).
